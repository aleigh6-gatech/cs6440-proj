clusters:
  - name: control
    backends:
      - control
  - name: app
    backends:
      - leader-address
      - replica-address
routes:
  - path: "/admin"
    clusters:
      - control
  - path: "*"
    clusters:
      - app
      - control
